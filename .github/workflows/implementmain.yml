name: main_branch_WF

on: 
    workflow_dispatch:
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: Set up Manual workflow for emojbranch
          run: echo "main branch"
        